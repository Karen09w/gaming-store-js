{
  "name": "sample",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "rm -rf ./dist && rm -rf ./.cache && parcel ./public/index.html",
    "build": "rm -rf ./build && parcel build ./public/index.html --out-dir build --public-url ./"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "parcel-plugin-static-files-copy": "^2.6.0",
    "sass": "^1.49.9"
  },
  "staticFiles": {
    "staticPath": [
      "./public"
    ]
  }
}
